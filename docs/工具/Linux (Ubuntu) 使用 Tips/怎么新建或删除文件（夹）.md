# 怎么新建或删除文件（夹）

```shell
mkdir /path/to/folder # 新增文件夹

rmdir /path/tp/empty_folder

rm -r /path/to/folder # -r 代表 recursively

rm -rf /path/to/folder #-rf 代表 recursively 和 force

sudo rm -rf /path/to/folder # 删除需要管理员权限的文件夹

touch file.txt # 如果文件不存在，就会新建；否则就会修改文件的时间戳
```
